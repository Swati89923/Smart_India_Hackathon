# Optional. Leave unset when using Expo Go on the same Wi-Fi — the app then
# uses your laptop's IP automatically (http://<laptop-ip>:4000/api).
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.15:4000/api
