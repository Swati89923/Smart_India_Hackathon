PORT=4000

# Google Gemini API Key for multimodal catalogue generation & image analysis
# Get a free key from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# Production only (this prototype uses a local data.json file instead —
# see src/db.js). Point these at a real PostgreSQL instance and swap
# db.js's load()/save() for pg queries to go to production.
DATABASE_URL=postgres://user:password@localhost:5432/shilpsaathi

